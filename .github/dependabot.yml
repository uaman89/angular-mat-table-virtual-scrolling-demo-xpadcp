version: 2

updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 1  # Prevents version update PRs
    groups:
      security-updates:
        applies-to: security-updates
        patterns:
          - "*"
